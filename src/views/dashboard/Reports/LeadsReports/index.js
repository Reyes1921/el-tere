import React, { useState } from 'react';

const LeadsReports = () => {
    const [reports, setReports] = useState([]);

    return (
        <div>
            <h1>Leads Reports Page</h1>
        </div>
    );
};

export default LeadsReports;

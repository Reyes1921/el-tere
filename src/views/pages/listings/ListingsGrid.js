import React from 'react';
import { Grid, Typography } from '@material-ui/core';

const ListingsGrid = () => {
    <div>
        <Typography>Here is the Listings Grid</Typography>
    </div>;
};

export default ListingsGrid;
